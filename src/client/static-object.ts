export class StaticObject {
    x: number;
    y: number;
    sprite: number;

    constructor(x: number, y: number, sprite: number) {
        this.x = x;
        this.y = y;
        this.sprite = sprite;
    }
}